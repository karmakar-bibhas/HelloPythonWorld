import converters

print(converters.kg_to_lbs(70))
print(converters.lbs_to_kg(70))
numbers = [35, 63, 85, 1, 60]
print(converters.find_max(numbers))

from converters import kg_to_lbs
print(kg_to_lbs(100))

from converters import find_max
numbers = [35, 63, 85, 1, 60]
print(find_max(numbers))
